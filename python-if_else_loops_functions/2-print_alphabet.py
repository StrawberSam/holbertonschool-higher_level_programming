#!/usr/bin/python3
for i in range(97,123):
    print("{}".format(chr(i)), end="")
# Affiche l'alphabet ASCII en minuscules sans retour à la ligne
# chr(i) prend un nombre entier et le convertit à son code ASCII
# "{}".format() insère dynamiquement une valeur dans une chaîne de caractères
